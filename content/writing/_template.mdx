---
title: "Your post title here"
date: "2026-09-04"
summary: "One or two sentences that show up in the list and link previews. Make it inviting."
---

Open with the point. Say the one thing this post is about, in plain language,
before you earn the reader's attention with detail.

## A section heading

Write in short paragraphs. Bold the phrase that matters, use `inline code` for
identifiers, and [link out](https://example.com) when it helps.

- A list when the items are parallel
- Another item
- One more

### A smaller subheading

Drop into a code block when showing something concrete:

```ts
export function greet(name: string) {
  return `Hey, ${name}!`;
}
```

Close with the takeaway — what should the reader do or remember?

<!--
HOW TO USE THIS TEMPLATE
1. Copy this file to content/writing/<slug>.mdx  (the filename becomes the URL).
2. Update title, date (YYYY-MM-DD), and summary.
3. Write the body in Markdown/MDX.
4. Files starting with "_" are ignored, so this template never gets published.
-->
